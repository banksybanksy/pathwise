DB_HOST=127.0.0.1
DB_PORT=3307
DB_USER=root
DB_PASSWORD=localdev
DB_NAME=pathwise

# Required for sessions (use a long random string in production)
SESSION_SECRET=change-this-to-a-long-random-string-at-least-32-chars

# Optional: set true behind HTTPS only
# COOKIE_SECURE=true

# Optional
# PORT=3000
